describe("Assign", function() {
  var assign;
  beforeEach(function(){
    assign = MPL.Assign();
  });
  it('should iterate through ext props and use the assignment operator to set them on the base obj');

  describe('Clone Assign and use alternate iterators', function(){});
});
